function Login(element) {
(element.innerText == "Login")
  alert("Logout")
  element.innerText = "Logout";
  }
var x = 0;
function Like(element) {x++;
  alert("the ninja was liked" + x);
  element.innerText == x + "Likes";
}
var y = 0;
function Likes(element) {y++;
  alert("the ninja was liked" + z);
  element.innerText == z + "Likes";
}
function hide(element) {
  element.remove();
}
